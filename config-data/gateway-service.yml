server:
  port: 8222
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
  instance:
    hostname: localhost
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: user-service
          uri: lb:http://USER-SERVICE
          predicates:
            - Path=/user/**
        - id: car-service
          uri: lb:http://CAR-SERVICE
          predicates:
            - Path=/car/**
        - id: bike-service
          uri: lb:http://BIKE-SERVICE
          predicates:
            - Path=/bike/**