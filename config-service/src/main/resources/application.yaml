server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: b8-zipkin-sleuth
          uri: https://github.com/ever5390/MS-Vehicles-Sales
          search-paths: config-data