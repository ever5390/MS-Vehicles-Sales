server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: gateway-fix
          uri: https://github.com/ever5390/MS-Vehicles-Sales
          search-paths: config-data