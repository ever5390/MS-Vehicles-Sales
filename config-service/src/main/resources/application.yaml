server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          default-label: b3-ms-config
          uri: https://github.com/ever5390/MS-Vehicles-Sales
          search-paths: config-data

application:
  name: config-service